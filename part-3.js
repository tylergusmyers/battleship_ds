// The GUI based grid should be a completely separate grid from the ships grid. 
// The ships grid will have X's to represent the ships and the display grid will check 
// if there is ship on the ship grid and then populate an X on the display grid.
// Separating the two is the key.